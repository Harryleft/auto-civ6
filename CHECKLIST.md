# 完成检查清单（每次任务交付前核对）

> 用法：任何任务交付前，逐项核对本清单。每一项回答「是 / 否 / 不适用(N/A)」；
> 未通过项先处理再交付；确实无法处理的，在交付说明中显式列出。
> 最终回复末尾必须报告：`清单核对：通过 x/16，N/A y`。
>
> 来源：由 AGENTS.md / CLAUDE.md 的规则蒸馏而成。本清单是防遗漏的唯一载体；
> 发现清单与规则冲突时，按更严格者执行，并向用户报告以修订清单。

## A. 通用（每次任务必查）

- [ ] **链路唯一**：没有并行运行第二个 FireTuner 客户端（Pi、Codex、独立 `civ-mcp`、连接测试或第二个 DSH MCP 客户端）？
- [ ] **信息边界**：只使用了明确查询过的游戏信息？运行模式以 `CIV_MCP_BELIEF_MODE` 和 `get_game_overview` 返回的 `RUNTIME POLICY` 为准，没有从文档或旧会话猜测？
- [ ] **证据归属**：原始工具结果没有复制进信念或日志（只保存规范化事实、指纹、决策/动作关联）？
- [ ] **返回语言**：面向用户的可读信息使用中文；协议字段名、工具名、参数名、错误码、路径和命令保持原样？
- [ ] **Git 提交**：改动已提交，摘要为中文 Conventional Commits，且只暂存了本次任务的文件？
- [ ] **验证相称**：运行了与改动相称的验证，且没有用离线测试冒充 FireTuner handshake / MCP 读取 / 真实回合动作？

## B. 代码改动（涉及代码时必查）

- [ ] **动作通道**：新增游戏动作走了 `execute_mutation` 通道并配了离线回归测试？没有绕开 `_logged` 管道？
- [ ] **事件溯源**：没有绕过事件流直接改内存态（BeliefEngine 只 append、墓碑不物理删除、epoch 作废旧授权）？
- [ ] **文档同步**：改了机制 / 启动 / 连接 / 恢复，就同步更新了对应文档（architecture-diagrams / belief-engine / governance-system / agent-startup / agent-recovery / DSH 集成）？
- [ ] **接口边界**：优先使用领域工具，没有把 `run_lua` 当作支持的游戏接口？

## C. 游戏回合（涉及对局时必查）

- [ ] **强制入口**：`get_game_overview` 是当前回合唯一强制入口，读取成功前没有执行游戏动作或 `end_turn`？
- [ ] **蛮族营地**：近城营地已优先清剿（营地是刷兵源头，击杀波次不等于消除刷兵源）？攻击前调用了 `get_combat_estimate`？
- [ ] **移动安全**：移动前检查了目的地及周围地块？长距离移动前使用了 `get_pathing_estimate`？
- [ ] **机会成本**：没有无计划囤积（闲置建造者、商路、金币、信仰）？每座城市的增长 / 生产 / 忠诚度 / 城墙 / 驻军都在视野内？
- [ ] **威胁升级**：对手 ≥2 倍军力、边境集结攻城单位或接近胜利条件时，提高了检查频率？
- [ ] **回合边界**：宣战回合没有攻击新敌人？`end_turn` 失败时先读取错误和当前状态，没有盲目循环重试？

---

核对结果：通过 ___ / 16　N/A ___　未通过项：___
